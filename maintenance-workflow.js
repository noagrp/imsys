import { auth, db } from './firebase-config.js';
import { addDoc, collection, doc, getDoc, getDocs, runTransaction, setDoc, updateDoc } from 'https://www.gstatic.com/firebasejs/11.0.1/firebase-firestore.js';
const byId=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),cls='w-full bg-slate-950 border border-slate-700 rounded-lg px-3 py-2.5 text-sm',now=()=>new Date().toISOString(),today=()=>now().slice(0,10);
const TYPES=['Inspection','Preventive Maintenance','Repair','Calibration','Certification / Recertification','Testing','Service','Other'],TASKS={Inspection:['Inspect condition','Test / verify','Record result'],'Preventive Maintenance':['Inspect condition','Service / replace consumables','Functional test'],Repair:['Diagnose issue','Repair / replace','Functional test'],Calibration:['Perform calibration','Verify tolerance','Record certificate / result'],'Certification / Recertification':['Inspect / test','Certification review','Record certificate'],Testing:['Prepare test','Perform test','Record result'],Service:['Inspect condition','Perform service','Functional test'],Other:['Perform work','Verify result']},PRIORITY=['In Transit','Maintenance','At Client','Not Available','At Supplier','Available'];
let inventory=[],settings=[],maintenance=[],suppliers=[],me=null,busy=false,lastMount=null;
function balances(i){return(Array.isArray(i.stockBalances)?i.stockBalances:[]).filter(x=>Number(x.qty||0)>0).map(x=>({qty:Number(x.qty||0),locationType:x.locationType||'warehouse',locationId:x.locationId||'',locationName:x.locationName||x.location||'Unknown',status:x.status||'Not Available'}));}
function summary(b){const p=b.filter(x=>x.qty>0);return!p.length?{status:'Not Available',location:'No Stock'}:{status:PRIORITY.find(s=>p.some(x=>x.status===s))||'Not Available',location:p.length===1?p[0].locationName:`${p.length} Locations`};}
function maintBalances(i){return balances(i).filter(b=>b.locationType==='maintenance'&&b.status==='Maintenance');}function sourceAllowed(b){return(b.locationType==='warehouse'&&(b.status==='Available'||b.status==='Not Available'))||(b.locationType==='client'&&b.status==='At Client');}function sendableItems(){return inventory.filter(i=>balances(i).some(sourceAllowed));}function activeItems(){return inventory.filter(i=>maintBalances(i).length);}function label(i){return`${i.alias||'No Alias'} · ${i.itemCode||'No IMS ID'} · ${i.name||''}`;}function latestJob(id){return maintenance.filter(x=>x.itemId===id).sort((a,b)=>String(b.createdAt||'').localeCompare(String(a.createdAt||'')))[0]||null;}function warehouses(){return settings.filter(x=>x.type==='warehouse'&&x.status!=='inactive');}function supplierName(x){return x.companyName||x.supplierName||'';}function providerFor(i){const b=maintBalances(i)[0];return{id:i.activeMaintenanceProviderId||b?.locationId||'',name:i.activeMaintenanceProviderName||b?.locationName||''};}
async function load(){const u=auth.currentUser;if(!u)throw Error('Sign in required.');const p=await getDoc(doc(db,'users',u.uid));if(!p.exists()||p.data().status!=='active')throw Error('Active user profile required.');me={...p.data(),email:p.data().email||u.email||''};const[a,b,c,d]=await Promise.all(['inventory','settings','maintenance_events','supplier_profiles'].map(n=>getDocs(collection(db,n))));inventory=a.docs.map(d=>({id:d.id,...d.data()}));settings=b.docs.map(d=>({id:d.id,...d.data()}));maintenance=c.docs.map(d=>({id:d.id,...d.data()}));suppliers=d.docs.map(d=>({id:d.id,...d.data()})).filter(x=>x.status!=='inactive').sort((a,b)=>supplierName(a).localeCompare(supplierName(b)));}
async function log(rec){await addDoc(collection(db,'operational_logs'),{logVersion:2,date:now(),performedBy:me.email,performedByRole:me.role,...rec});}function baseLog(i,e={}){return{itemId:i.id,itemCode:i.itemCode,itemAlias:i.alias||'',itemName:i.name,category:i.category||'',supplierId:i.supplierId||'',supplierName:i.supplierName||'',clientId:'',clientName:'',unit:i.unit||'',...e};}async function audit(action,i,before,after,remark='',targetId='',metadata={}){await addDoc(collection(db,'audit_traces'),{traceVersion:3,actionType:action,module:'Maintenance',targetType:'maintenance',targetName:i.alias||i.itemCode,targetId,summary:`${action.replace(/_/g,' ')}: ${label(i)}`,beforeValue:before,afterValue:after,changedFields:['maintenance'],remark,metadata:{itemId:i.id,itemCode:i.itemCode,itemAlias:i.alias||'',...metadata},performedBy:me.email,performedByRole:me.role,performedAt:now()});}
function setBusy(btn,on,text='Saving...'){if(!btn)return;if(!btn.dataset.label)btn.dataset.label=btn.textContent;btn.disabled=on;btn.textContent=on?text:btn.dataset.label;}function selectedIds(except){return new Set([...document.querySelectorAll('.mwSendItem')].filter(x=>x!==except).map(x=>x.value).filter(Boolean));}function itemOptions(sel=null,current=''){const used=selectedIds(sel);return'<option value="">-</option>'+sendableItems().filter(i=>!used.has(i.id)||i.id===current).map(i=>`<option value="${i.id}" ${i.id===current?'selected':''}>${esc(i.alias||'No Alias')}</option>`).join('');}function refreshItemOptions(){document.querySelectorAll('.mwSendItem').forEach(s=>{const v=s.value;s.innerHTML=itemOptions(s,v);s.value=v;});}function sourceOptions(id){const i=inventory.find(x=>x.id===id);return'<option value="">-</option>'+(i?balances(i).map((b,n)=>({b,n})).filter(x=>sourceAllowed(x.b)).map(({b,n})=>`<option value="${n}">${esc(b.locationName)} · ${b.qty} ${esc(i.unit||'')} · ${esc(b.status)}</option>`).join(''):'');}
function rowHtml(){return`<div class="mwSendRow border border-slate-800 rounded-xl p-3"><div class="grid sm:grid-cols-2 xl:grid-cols-[1fr_1.4fr_1.2fr_.6fr_auto] gap-2 items-end"><label class="text-xs text-cyan-300">Alias<select class="mwSendItem ${cls} mt-1">${itemOptions()}</select></label><div class="mwFetched text-xs border border-slate-800 rounded-lg p-2 min-h-[58px] text-slate-400">Select Alias.</div><label class="text-xs text-slate-400">Source<select class="mwSendSource ${cls} mt-1"><option value="">-</option></select></label><label class="text-xs text-slate-400">Units<input class="mwSendQty ${cls} mt-1" type="number" min="1" step="1" value="1"></label><button type="button" class="mwSendRemove bg-slate-800 px-3 py-2.5 rounded-lg text-xs">Remove</button></div></div>`;}function bindRow(r){const s=r.querySelector('.mwSendItem'),src=r.querySelector('.mwSendSource');s.onchange=()=>{const i=inventory.find(x=>x.id===s.value);r.querySelector('.mwFetched').innerHTML=i?`<b>${esc(i.name||'')}</b><div class="font-mono">${esc(i.itemCode||'')}</div>`:'Select Alias.';src.innerHTML=sourceOptions(s.value);refreshItemOptions();};r.querySelector('.mwSendRemove').onclick=()=>{if(document.querySelectorAll('.mwSendRow').length===1)return alert('At least one maintenance line is required.');r.remove();refreshItemOptions();};}function addRow(){const w=byId('mwSendRows');w.insertAdjacentHTML('beforeend',rowHtml());bindRow(w.lastElementChild);refreshItemOptions();}function lines(){return[...document.querySelectorAll('.mwSendRow')].map((r,n)=>{const item=inventory.find(x=>x.id===r.querySelector('.mwSendItem').value),raw=r.querySelector('.mwSendSource').value,idx=raw===''?NaN:Number(raw),balance=item&&Number.isInteger(idx)?balances(item)[idx]:null;return{lineNo:n+1,item,balance,qty:Number(r.querySelector('.mwSendQty').value||0)};}).filter(x=>x.item&&x.balance&&sourceAllowed(x.balance));}function providerOptions(){return'<option value="">-</option>'+suppliers.map(x=>`<option value="${x.id}" data-name="${esc(supplierName(x))}">${esc(supplierName(x))}</option>`).join('');}
function shellHtml(){return`<div id="maintenanceWorkflowForm" class="space-y-5"><section class="border border-slate-800 rounded-2xl p-4 space-y-3"><div class="font-bold">Send to Maintenance — Multi Item / Multi Unit / Multi Location</div><div class="grid sm:grid-cols-2 gap-2"><label class="text-xs text-slate-400">Maintenance Supplier / Service Provider<select id="mwSendProvider" class="${cls} mt-1">${providerOptions()}</select></label><label class="text-xs text-slate-400">Remark<input id="mwSendRemark" class="${cls} mt-1"></label></div><div class="flex justify-end"><button id="mwAddSendRow" type="button" class="bg-slate-700 px-3 py-2 rounded-lg text-xs">+ Add Item / Location</button></div><div id="mwSendRows" class="space-y-2"></div><button id="mwSendBatch" type="button" class="w-full bg-amber-700 py-2.5 rounded-lg text-sm font-bold">Start Maintenance Movements</button></section><section class="border border-slate-800 rounded-2xl p-4 space-y-3"><div class="font-bold">Maintenance Job / Result / Return</div><label class="block text-xs text-slate-400">Alias · IMS Item ID · Item Name<select id="mwItem" class="${cls} mt-1"><option value="">-</option>${activeItems().map(i=>`<option value="${i.id}">${esc(label(i))}</option>`).join('')}</select></label><div id="mwBody"></div></section></div>`;}
function openHtml(i){const p=providerFor(i);return`<div class="border border-slate-800 rounded-xl p-3 space-y-3"><div class="font-semibold">Open Maintenance Job</div><div class="grid sm:grid-cols-2 gap-2"><label class="text-xs text-slate-400">Maintenance Type<select id="mwType" class="${cls} mt-1">${TYPES.map(x=>`<option>${esc(x)}</option>`).join('')}</select></label><label class="text-xs text-slate-400">Start Date<input id="mwStart" type="date" value="${today()}" class="${cls} mt-1"></label></div><label class="text-xs text-slate-400">Provider<input id="mwProvider" value="${esc(p.name)}" readonly class="${cls} mt-1 opacity-70"></label><label class="text-xs text-slate-400">Tasks<textarea id="mwTasks" rows="4" class="${cls} mt-1">${TASKS.Inspection.join('\n')}</textarea></label><label class="text-xs text-slate-400">Remark<textarea id="mwRemark" rows="2" class="${cls} mt-1"></textarea></label><button id="mwOpen" type="button" class="w-full bg-amber-600 py-2.5 rounded-lg text-sm font-bold">Open Maintenance</button></div>`;}function taskRows(j,ro=false){return(j.tasks||[]).map((t,n)=>`<div class="grid grid-cols-[auto_minmax(0,1fr)_145px] gap-2 items-center border border-slate-800 rounded-lg p-2"><input class="mwTaskDone" data-i="${n}" type="checkbox" ${t.done?'checked':''} ${ro?'disabled':''}><div class="text-xs">${esc(t.name)}</div><input class="mwTaskDate ${cls}" data-i="${n}" type="date" value="${esc(t.date||'')}" ${ro?'disabled':''}></div>`).join('');}function resultHtml(j){return!j.maintenanceResult?`<div class="border-t border-slate-800 pt-3 space-y-2"><textarea id="mwResultRemark" rows="2" class="${cls}" placeholder="Failure reason required for Fail"></textarea><div class="grid grid-cols-2 gap-2"><button id="mwPass" type="button" class="bg-emerald-700 py-2 rounded-lg text-xs">Pass</button><button id="mwFail" type="button" class="bg-red-700 py-2 rounded-lg text-xs">Fail</button></div></div>`:`<div class="border-t border-slate-800 pt-3"><b>${esc(j.maintenanceResult)}</b><div class="text-xs text-slate-500">${esc(j.resultRemark||'')}</div></div>`;}function releaseHtml(i,j){if(!j.maintenanceResult)return'';return`<div class="border-t border-slate-800 pt-3 space-y-2">${j.maintenanceResult==='Fail'?'<button id="mwKeepMaintenance" type="button" class="w-full bg-amber-700 py-2 rounded-lg text-xs">Keep Under Maintenance</button>':''}<div class="grid sm:grid-cols-2 gap-2"><label class="text-xs text-slate-400">Maintenance Source<select id="mwReleaseSource" class="${cls} mt-1">${maintBalances(i).map((b,n)=>`<option value="${n}">${esc(b.locationName)} · ${b.qty}</option>`).join('')}</select></label><label class="text-xs text-slate-400">Return Warehouse<select id="mwWarehouse" class="${cls} mt-1"><option value="">-</option>${warehouses().map(w=>`<option value="${w.id}" data-name="${esc(w.value)}">${esc(w.value)}</option>`).join('')}</select></label></div><label class="text-xs text-slate-400">Units<input id="mwReleaseQty" type="number" min="1" step="1" value="${maintBalances(i)[0]?.qty||1}" class="${cls} mt-1"></label><label class="text-xs text-slate-400">Remark<input id="mwReleaseRemark" class="${cls} mt-1"></label><button id="mwRelease" type="button" class="w-full bg-cyan-700 py-2.5 rounded-lg text-sm">${j.maintenanceResult==='Pass'?'Return — Available':'Return — Not Available'}</button></div>`;}function jobHtml(i,j){const done=j.eventStatus==='Completed';return`<div class="border border-slate-800 rounded-xl p-3 space-y-3"><div><b>${esc(label(i))}</b><div class="text-xs text-slate-500">${esc(j.eventType||'Maintenance')} · Provider: ${esc(j.provider||providerFor(i).name||'')}</div></div>${taskRows(j,done)}${done?resultHtml(j)+releaseHtml(i,j):`<div class="flex justify-end gap-2"><button id="mwSaveProgress" type="button" class="bg-slate-700 px-4 py-2 rounded-lg text-xs">Save Progress</button><button id="mwComplete" type="button" class="bg-emerald-700 px-4 py-2 rounded-lg text-xs">Complete</button></div><input id="mwCompleteDate" type="date" value="${today()}" class="${cls}">`}</div>`;}
function collectTasks(j){return(j.tasks||[]).map((t,n)=>({name:t.name,done:document.querySelector(`.mwTaskDone[data-i="${n}"]`)?.checked||false,date:document.querySelector(`.mwTaskDate[data-i="${n}"]`)?.value||''}));}async function refresh(selected=''){await load();const r=byId('maintenanceWorkflowForm');if(!r)return;r.outerHTML=shellHtml();bind();if(selected){byId('mwItem').value=selected;await renderSelected();}}function bind(){byId('mwAddSendRow').onclick=addRow;byId('mwSendBatch').onclick=sendBatch;byId('mwItem').onchange=renderSelected;addRow();}async function renderSelected(){const i=inventory.find(x=>x.id===byId('mwItem')?.value),body=byId('mwBody');if(!body)return;if(!i){body.innerHTML='';return;}const j=latestJob(i.id);body.innerHTML=j?jobHtml(i,j):openHtml(i);if(!j){byId('mwType').onchange=()=>byId('mwTasks').value=(TASKS[byId('mwType').value]||TASKS.Other).join('\n');byId('mwOpen').onclick=()=>openJob(i);}else if(j.eventStatus==='Completed'){if(!j.maintenanceResult){byId('mwPass').onclick=()=>setResult(i,j,'Pass');byId('mwFail').onclick=()=>setResult(i,j,'Fail');}else{byId('mwRelease').onclick=()=>release(i,j);if(j.maintenanceResult==='Fail')byId('mwKeepMaintenance').onclick=()=>keep(i,j);}}else{byId('mwSaveProgress').onclick=()=>progress(i,j,false);byId('mwComplete').onclick=()=>progress(i,j,true);}}
async function sendBatch(){if(busy)return;const ps=byId('mwSendProvider'),providerId=ps.value,provider=ps.selectedOptions?.[0]?.dataset?.name||'',remark=byId('mwSendRemark').value.trim(),ls=lines(),btn=byId('mwSendBatch');if(!providerId)return alert('Select a maintenance supplier / service provider.');if(!ls.length)return alert('Add at least one valid item and source.');if(new Set(ls.map(x=>x.item.id)).size!==ls.length)return alert('The same item cannot be selected twice.');for(const l of ls)if(!Number.isInteger(l.qty)||l.qty<1||l.qty>l.balance.qty)return alert(`Line ${l.lineNo}: invalid units.`);busy=true;setBusy(btn,true,'Starting...');try{const batchId=doc(collection(db,'movements')).id,createdAt=now(),refs=ls.map(()=>doc(collection(db,'movements')));await runTransaction(db,async tx=>{const state=new Map();for(const l of ls){const s=await tx.get(doc(db,'inventory',l.item.id));if(!s.exists())throw Error('Item missing.');state.set(l.item.id,{balances:balances(s.data())});}for(let n=0;n<ls.length;n++){const l=ls[n],st=state.get(l.item.id),src=st.balances.find(b=>b.locationType===l.balance.locationType&&b.locationId===l.balance.locationId&&b.locationName===l.balance.locationName&&b.status===l.balance.status);if(!src||src.qty<l.qty)throw Error('Source stock changed.');src.qty-=l.qty;const mr=refs[n];st.balances.push({qty:l.qty,locationType:'transit',locationId:mr.id,locationName:`Transit to ${provider}`,status:'In Transit'});tx.set(mr,{movementId:mr.id,movementBatchId:batchId,itemId:l.item.id,itemCode:l.item.itemCode,itemAlias:l.item.alias||'',itemNameSnapshot:l.item.name,action:'SEND_MAINTENANCE',actionLabel:'Send to Maintenance',fromType:l.balance.locationType,fromId:l.balance.locationId||'',fromName:l.balance.locationName,toType:'maintenance',toId:providerId,toName:provider,toStatus:'Maintenance',qty:l.qty,unit:l.item.unit||'',remark,status:'in_transit',createdAt,createdBy:me.email,createdByRole:me.role});}for(const[id,st]of state){const clean=st.balances.filter(b=>b.qty>0),sum=summary(clean);tx.update(doc(db,'inventory',id),{stockBalances:clean,status:sum.status,currentLocation:sum.location,activeMaintenanceProviderId:providerId,activeMaintenanceProviderName:provider,lastEditedBy:me.email,lastEditedAt:createdAt});}});for(let n=0;n<ls.length;n++){const l=ls[n],mr=refs[n];await log(baseLog(l.item,{activity:'SEND_MAINTENANCE',status:'In Transit',fromName:l.balance.locationName,toName:provider,qty:l.qty,movementId:mr.id,movementBatchId:batchId,remark}));await audit('SEND_MAINTENANCE',l.item,{status:l.balance.status},{status:'In Transit',providerId,provider},remark,mr.id,{movementBatchId:batchId});}alert('Maintenance movement started.');await refresh();}catch(e){alert('Send to maintenance failed: '+e.message);}finally{busy=false;setBusy(btn,false);}}
async function openJob(i){if(busy)return;const tasks=byId('mwTasks').value.split('\n').map(x=>x.trim()).filter(Boolean),p=providerFor(i),btn=byId('mwOpen');if(!tasks.length)return alert('Add at least one task.');if(!p.name)return alert('Provider is missing from the original maintenance movement.');busy=true;setBusy(btn,true);try{const ref=doc(collection(db,'maintenance_events')),rec={eventId:ref.id,itemId:i.id,itemCode:i.itemCode,itemAliasSnapshot:i.alias||'',itemNameSnapshot:i.name,eventType:byId('mwType').value,eventStatus:'Open',maintenanceFrom:byId('mwStart').value,providerId:p.id,provider:p.name,remark:byId('mwRemark').value.trim(),tasks:tasks.map(name=>({name,done:false,date:''})),createdBy:me.email,createdByRole:me.role,createdAt:now()};await setDoc(ref,rec);await log(baseLog(i,{activity:'MAINTENANCE_OPEN',status:'Maintenance',fromName:p.name,toName:p.name,maintenanceEventId:ref.id,remark:rec.remark}));await audit('OPEN_MAINTENANCE',i,null,{provider:p.name,type:rec.eventType},rec.remark,ref.id);await refresh(i.id);}catch(e){alert('Open maintenance failed: '+e.message);}finally{busy=false;setBusy(btn,false);}}
async function progress(i,j,complete){if(busy)return;const tasks=collectTasks(j),btn=complete?byId('mwComplete'):byId('mwSaveProgress');if(complete&&!tasks.every(t=>t.done&&t.date))return alert('Complete every task with a date.');busy=true;setBusy(btn,true);try{const patch={tasks,eventStatus:complete?'Completed':'Open',updatedAt:now(),updatedBy:me.email};if(complete)Object.assign(patch,{completedDate:byId('mwCompleteDate').value,completedAt:now(),completedBy:me.email});await updateDoc(doc(db,'maintenance_events',j.id),patch);await log(baseLog(i,{activity:complete?'MAINTENANCE_COMPLETE':'MAINTENANCE_PROGRESS',status:'Maintenance',toName:j.provider||providerFor(i).name,maintenanceEventId:j.id}));await refresh(i.id);}catch(e){alert('Maintenance update failed: '+e.message);}finally{busy=false;setBusy(btn,false);}}
async function setResult(i,j,result){if(busy)return;const remark=byId('mwResultRemark').value.trim(),btn=result==='Pass'?byId('mwPass'):byId('mwFail');if(result==='Fail'&&!remark)return alert('Enter failure reason.');busy=true;setBusy(btn,true);try{await updateDoc(doc(db,'maintenance_events',j.id),{maintenanceResult:result,resultRemark:remark,resultAt:now(),resultBy:me.email,updatedAt:now(),updatedBy:me.email});await log(baseLog(i,{activity:'MAINTENANCE_RESULT',status:'Maintenance',toName:j.provider||providerFor(i).name,maintenanceResult:result,remark}));await refresh(i.id);}finally{busy=false;setBusy(btn,false);}}
async function keep(i,j){if(busy)return;const btn=byId('mwKeepMaintenance');busy=true;setBusy(btn,true);try{await updateDoc(doc(db,'maintenance_events',j.id),{failedDisposition:'Keep in Maintenance',failedDispositionAt:now(),failedDispositionBy:me.email});await refresh(i.id);}finally{busy=false;setBusy(btn,false);}}
async function release(i,j){if(busy)return;const src=maintBalances(i)[Number(byId('mwReleaseSource').value)],qty=Number(byId('mwReleaseQty').value||0),wh=byId('mwWarehouse'),toId=wh.value,toName=wh.selectedOptions?.[0]?.dataset?.name||'',remark=byId('mwReleaseRemark').value.trim(),finalStatus=j.maintenanceResult==='Pass'?'Available':'Not Available',btn=byId('mwRelease');if(!src||!Number.isInteger(qty)||qty<1||qty>src.qty||!toId)return alert('Select valid source, units and warehouse.');busy=true;setBusy(btn,true,'Starting return...');try{const mr=doc(collection(db,'movements')),createdAt=now();await runTransaction(db,async tx=>{const ref=doc(db,'inventory',i.id),s=await tx.get(ref),bal=balances(s.data()),source=bal.find(b=>b.locationType==='maintenance'&&b.status==='Maintenance'&&b.locationName===src.locationName);if(!source||source.qty<qty)throw Error('Maintenance stock changed.');source.qty-=qty;bal.push({qty,locationType:'transit',locationId:mr.id,locationName:`Transit to ${toName}`,status:'In Transit'});const clean=bal.filter(b=>b.qty>0),sum=summary(clean),still=clean.some(b=>b.locationType==='maintenance'&&b.status==='Maintenance');tx.update(ref,{stockBalances:clean,status:sum.status,currentLocation:sum.location,activeMaintenanceProviderId:still?(s.data().activeMaintenanceProviderId||''):'',activeMaintenanceProviderName:still?(s.data().activeMaintenanceProviderName||''):'',lastEditedAt:createdAt,lastEditedBy:me.email});tx.set(mr,{movementId:mr.id,itemId:i.id,itemCode:i.itemCode,itemAlias:i.alias||'',action:'RETURN_MAINTENANCE',fromType:'maintenance',fromName:src.locationName,toType:'warehouse',toId,toName,toStatus:finalStatus,qty,unit:i.unit||'',remark,status:'in_transit',maintenanceResult:j.maintenanceResult,maintenanceEventId:j.id,createdAt,createdBy:me.email,createdByRole:me.role});});await updateDoc(doc(db,'maintenance_events',j.id),{failedDisposition:j.maintenanceResult==='Fail'?'Return to Warehouse — Not Available':'Return to Warehouse — Available',releaseMovementId:mr.id,updatedAt:createdAt,updatedBy:me.email});await log(baseLog(i,{activity:'RETURN_MAINTENANCE',status:'In Transit',fromName:src.locationName,toName,qty,movementId:mr.id,maintenanceResult:j.maintenanceResult,remark}));alert(`Return started. Arrival status: ${finalStatus}.`);await refresh();}catch(e){alert('Maintenance return failed: '+e.message);}finally{busy=false;setBusy(btn,false);}}
async function mount(){const core=byId('maintenanceForm');if(!core||core===lastMount||byId('maintenanceWorkflowForm'))return;lastMount=core;try{await load();}catch(e){console.warn(e);return;}const w=document.createElement('div');w.innerHTML=shellHtml();core.replaceWith(w.firstElementChild);bind();}let timer;new MutationObserver(()=>{clearTimeout(timer);timer=setTimeout(mount,35);}).observe(document.body,{childList:true,subtree:true});mount();