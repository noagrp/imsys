import { auth, db } from './firebase-config.js';
import { addDoc, collection, doc, getDoc, getDocs, setDoc } from 'https://www.gstatic.com/firebasejs/11.0.1/firebase-firestore.js';
const ROLE=window.IMS_ROLE||'admin',byId=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),norm=s=>String(s??'').trim().replace(/\s+/g,' ').toLowerCase(),now=()=>new Date().toISOString(),cls='w-full bg-slate-950 border border-slate-700 rounded-lg px-3 py-2.5 text-sm';let settings=[],suppliers=[],inventory=[],saving=false,lastCore=null;
async function load(){const[a,b,c]=await Promise.all([getDocs(collection(db,'settings')),getDocs(collection(db,'supplier_profiles')),getDocs(collection(db,'inventory'))]);settings=a.docs.map(d=>({id:d.id,...d.data()}));suppliers=b.docs.map(d=>({id:d.id,...d.data()})).filter(x=>x.status!=='inactive');inventory=c.docs.map(d=>({id:d.id,...d.data()}));}function active(t){return settings.filter(x=>x.type===t&&x.status!=='inactive')}async function profile(){const u=auth.currentUser;if(!u)throw new Error('Sign in required.');const s=await getDoc(doc(db,'users',u.uid));if(!s.exists()||s.data().status!=='active')throw new Error('Active user profile required.');return{email:s.data().email||u.email||'',role:s.data().role||ROLE}}function code(ref){return`ITM-${ref.id.slice(0,10).toUpperCase()}`}function selected(id){const e=byId(id),o=e?.selectedOptions?.[0];return{id:e?.value||'',value:o?.dataset?.v||o?.textContent?.trim()||''}}function options(t,l){return`<option value="">-- ${l} --</option>${active(t).map(x=>`<option value="${x.id}" data-v="${esc(x.value)}">${esc(x.value)}</option>`).join('')}`}
function aliases(count){const list=(byId('batchAliases')?.value||'').split('\n').map(x=>x.trim()).filter(Boolean);if(list.length!==count)throw new Error(`Enter exactly ${count} Alias value(s), one per unit.`);const dup=list.find((x,i)=>list.findIndex(y=>norm(y)===norm(x))!==i);if(dup)throw new Error(`Duplicate Alias in this batch: ${dup}`);const used=new Set(inventory.map(x=>norm(x.alias||'')).filter(Boolean)),clash=list.find(x=>used.has(norm(x)));if(clash)throw new Error(`Alias already exists: ${clash}`);return list}function initialWrap(){const atSupplier=byId('initialPosition')?.value==='supplier',w=byId('initialLocationWrap');if(!w)return;if(atSupplier){const sid=byId('itemSupplier')?.value||'',s=suppliers.find(x=>x.id===sid);w.innerHTML=`<label class="text-xs text-slate-400 sm:col-span-2">Initial Supplier Location<input id="initialLocation" readonly class="${cls} mt-1" value="${esc(s?.companyName||s?.supplierName||'Select supplier above')}"></label>`}else w.innerHTML=`<label class="text-xs text-slate-400 sm:col-span-2">Initial Warehouse / Location<select id="initialLocation" required class="${cls} mt-1">${options('warehouse','Warehouse / Location')}</select></label>`}
function formHtml(){return`<form id="registerForm" class="grid sm:grid-cols-2 gap-3" data-latest-registration="1"><div class="sm:col-span-2 text-xs text-slate-500">Every physical unit gets its own permanent IMS Item ID. Register many units together; enter one Alias per unit.</div><label class="text-xs text-slate-400">Item Type<select id="itemType" class="${cls} mt-1"><option>Equipment</option><option>Part</option></select></label><label class="text-xs text-slate-400">Units to Register<input id="itemQty" type="number" min="1" step="1" value="1" required class="${cls} mt-1"></label><label class="text-xs text-slate-400">Item Name<input id="itemName" required class="${cls} mt-1"></label><label class="text-xs text-slate-400">Category<select id="itemCategory" required class="${cls} mt-1">${options('category','Category')}</select></label><label class="text-xs text-slate-400">Unit<select id="itemUnit" required class="${cls} mt-1">${options('unit','Unit')}</select></label><label class="text-xs text-slate-400">Supplier<select id="itemSupplier" class="${cls} mt-1"><option value="">-- Supplier --</option>${suppliers.map(x=>`<option value="${x.id}">${esc(x.companyName||x.supplierName||'')}</option>`).join('')}</select></label><label class="text-xs text-slate-400">Brand<select id="itemBrand" class="${cls} mt-1">${options('brand','Brand')}</select></label><label class="text-xs text-slate-400">Model<select id="itemModel" class="${cls} mt-1">${options('model','Model')}</select></label><label class="text-xs text-slate-400">Grade<select id="itemGrade" class="${cls} mt-1">${options('grade','Grade')}</select></label><label class="text-xs text-slate-400">Specification<select id="itemSpecification" class="${cls} mt-1">${options('specification','Specification')}</select></label><label class="text-xs text-slate-400">Initial Position<select id="initialPosition" class="${cls} mt-1"><option value="warehouse">Available at Warehouse</option><option value="supplier">At Supplier</option></select></label><div id="initialLocationWrap" class="sm:col-span-2"></div><label class="text-xs font-semibold text-cyan-300 sm:col-span-2">Alias — one per unit<textarea id="batchAliases" rows="4" class="${cls} mt-1" placeholder="EQ-001\nEQ-002\nEQ-003"></textarea></label><label class="text-xs text-slate-400 sm:col-span-2">Remark<textarea id="itemRemark" rows="2" class="${cls} mt-1"></textarea></label><button id="registerSave" class="sm:col-span-2 w-full bg-red-600 py-2.5 rounded-lg text-sm font-bold">Register Item(s)</button><div id="registrationResult" class="sm:col-span-2"></div></form>`}
async function save(e){e.preventDefault();if(saving)return;saving=true;const btn=byId('registerSave');btn.disabled=true;btn.textContent='Saving...';try{await load();const user=await profile(),qty=Number(byId('itemQty').value||0);if(!Number.isInteger(qty)||qty<1||qty>500)throw new Error('Units must be a whole number from 1 to 500.');const aliasList=aliases(qty),name=byId('itemName').value.trim(),unit=selected('itemUnit'),cat=selected('itemCategory'),position=byId('initialPosition').value,sid=byId('itemSupplier').value,s=suppliers.find(x=>x.id===sid);if(!name||!unit.value||!cat.value)throw new Error('Complete Item Name, Category and Unit.');if(position==='supplier'&&!s)throw new Error('Select supplier.');const loc=position==='supplier'?{id:sid,value:s.companyName||s.supplierName||''}:selected('initialLocation');if(!loc.value)throw new Error('Select initial location.');const status=position==='supplier'?'At Supplier':'Available',batch=doc(collection(db,'registration_batches')),createdAt=now(),b=selected('itemBrand'),m=selected('itemModel'),g=selected('itemGrade'),sp=selected('itemSpecification');await setDoc(batch,{batchId:batch.id,quantity:qty,recordCount:qty,itemName:name,category:cat.value,unit:unit.value,initialPosition:position,initialLocationId:loc.id,initialLocation:loc.value,createdBy:user.email,createdByRole:user.role,createdAt});const created=[];for(let n=0;n<qty;n++){const ref=doc(collection(db,'inventory')),itemCode=code(ref),rec={type:byId('itemType').value,itemCode,alias:aliasList[n],name,category:cat.value,quantity:1,unit:unit.value,status,currentLocation:loc.value,supplierId:sid||'',supplierName:s?.companyName||s?.supplierName||'',stockBalances:[{qty:1,locationType:position,locationId:loc.id,locationName:loc.value,status}],brandId:b.id,brand:b.value,modelId:m.id,model:m.value,gradeId:g.id,grade:g.value,specificationId:sp.id,specification:sp.value,remark:byId('itemRemark').value.trim(),registrationBatchId:batch.id,registrationBatchIndex:n+1,createdBy:user.email,createdAt,lastEditedBy:null,lastEditedAt:null};await setDoc(ref,rec);const item={id:ref.id,...rec};await addDoc(collection(db,'operational_logs'),{logVersion:2,date:createdAt,activity:'REGISTER_ITEM',activityLabel:'Register Item',status,toType:position,toName:loc.value,qty:1,unit:item.unit,itemId:item.id,itemCode:item.itemCode,itemAlias:item.alias,itemName:item.name,category:item.category,supplierId:item.supplierId,supplierName:item.supplierName,registrationBatchId:batch.id,performedBy:user.email,performedByRole:user.role,remark:item.remark});created.push(item)}inventory.push(...created);byId('registrationResult').innerHTML=`<div class="rounded-xl border border-emerald-800 p-3 text-xs space-y-2"><div class="space-y-1">${created.map(x=>`<div><b class="text-cyan-300">${esc(x.alias)}</b> · ${esc(x.itemCode)} · ${esc(x.name)}</div>`).join('')}</div><button id="registrationDone" type="button" class="w-full sm:w-auto bg-slate-700 hover:bg-slate-600 px-4 py-2 rounded-lg text-xs font-semibold">Done</button></div>`;byId('registrationDone').onclick=()=>location.reload();byId('batchAliases').value='';byId('itemQty').value='1';}catch(err){alert('Registration failed: '+(err?.message||err));}finally{btn.disabled=false;btn.textContent='Register Item(s)';saving=false}}
async function mount(){const core=byId('registerForm');if(!core||core.dataset.latestRegistration==='1'||core===lastCore)return;lastCore=core;try{await load()}catch{return}const w=document.createElement('div');w.innerHTML=formHtml();core.replaceWith(w.firstElementChild);byId('initialPosition').onchange=initialWrap;byId('itemSupplier').onchange=()=>{if(byId('initialPosition').value==='supplier')initialWrap()};byId('registerForm').onsubmit=save;initialWrap()}let t;new MutationObserver(()=>{clearTimeout(t);t=setTimeout(mount,30)}).observe(document.body,{childList:true,subtree:true});mount();